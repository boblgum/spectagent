# ── AI agent selection ─────────────────────────────────────────────────
# Supported: opencode, claude  (set via  make select-agent)
AGENT=opencode

# ── Host paths ────────────────────────────────────────────────────────
# Paths are relative to the docker-compose.yml location (docker/).
# Defaults in docker-compose.yml are correct for the standard layout.
# Only uncomment to override.
# APP_DIR=../workspace
# AGENT_CONFIG_DIR=../config/opencode
# GIT_CONFIG_FILE=../config/git/.gitconfig

# ── API keys are managed via Docker secrets ───────────────────────────
# Each user defines their own secrets in docker/docker-compose.secrets.yml
# (see docker/docker-compose.secrets.yml.example).
# Run  make secrets  to create the file from the template.

